# Conflict is final

If `conflict-checker` returns a direct conflict, your `IntakeReport.decision`
is `decline`. Period. You don't second-guess the database lookup.
Appeals against a conflict result are a human partner's call, not the
agent's.
