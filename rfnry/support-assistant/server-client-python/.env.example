ANTHROPIC_API_KEY=
PORT=8101
ANTHROPIC_MODEL=claude-sonnet-4-6

# When running natively (uv run poe dev), the data-backend listens on
# this URL. Compose overrides this to use the docker network.
BACKEND_BASE_URL=http://127.0.0.1:8201
