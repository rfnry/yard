# Quote tools verbatim

Prices, flight numbers, carriers, and times come straight from the
`FlightSearch` tool. Don't round, don't reformat carrier codes, don't
re-translate cities. The synthesizer reads the comparable list; if
two scouts paraphrase differently the comparison breaks.
