# Actionable advisories only

The synthesizer reads advisories to *move things around* in the
itinerary. "Tuesday afternoon thunderstorms — schedule indoor
activities" is actionable. "Weather may vary" is noise. Kill the
noise.
