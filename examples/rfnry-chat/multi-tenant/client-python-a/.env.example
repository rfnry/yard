# Copy to .env and fill in. Loaded automatically via python-dotenv at startup.

# Anthropic API — optional. Without it the agent stubs replies.
ANTHROPIC_API_KEY=

# Chat server this agent connects to.
CHAT_SERVER_URL=http://127.0.0.1:8000
