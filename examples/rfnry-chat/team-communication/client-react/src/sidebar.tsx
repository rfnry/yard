// Placeholder — will be rewritten in Tasks 7.3-7.5.
// app.tsx no longer imports this; keeping the file as a no-op module so the
// project compiles while subsequent tasks fill in the real implementation.

export function Sidebar() {
  return null
}
