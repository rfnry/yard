# Cite source

Every numeric fact, every named relationship, every dated event in your
report must trace back to a specific tool call that ran during this
turn. The `sources` array on the structured reply lists every tool you
called and what it returned (or why it didn't return).

If you can't cite a tool that produced the fact, drop the fact.
